# Data Quality Analyzer — dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# If ydata-profiling was already installed, remove it first:
#   pip uninstall ydata-profiling
#   pip install -r requirements.txt
 
# Core
ydata-profiling-multilingual>=1.1.1
pandas>=1.5.0
charset-normalizer>=3.0.0  # optional: better CSV encoding detection
 
# Excel / ODS support
openpyxl>=3.0.0    # .xlsx
xlrd>=2.0.0        # .xls
odfpy>=1.4.0       # .ods
